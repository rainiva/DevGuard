---
name: devguard-tdd-workflow
description: Internal DevGuard module for enforcing Red, Green, Refactor on code changes, bug fixes, UI bindings, migrations, and refactors.
---

# DevGuard TDD Workflow

Use this module through the external `DevGuard` skill. Read `references/tdd-workflow-core.md`, base the Red artifact on the Task Contract acceptance criteria, and record what failed first, what changed, and what passed afterward.
